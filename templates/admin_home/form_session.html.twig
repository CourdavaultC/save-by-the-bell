{% extends 'base.html.twig' %}

{% block title %}
    {% if app.request.get('_route') == "edit_session" %}Editer{% else %}Ajouter{% endif %} une session
{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        <div>
            <h1 class="text-center">{% if app.request.get('_route') == "edit_session" %}Editer{% else %}Ajouter{% endif %} une session</h1>
        </div>

        <div>
            {{ form(form) }}
        </div>
    </div>


{% endblock %}