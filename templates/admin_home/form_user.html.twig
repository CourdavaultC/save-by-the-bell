{% extends 'base.html.twig' %}

{% block title %}
    {% if app.request.get('_route') == "edit_user" %}Editer{% else %}Ajouter{% endif %} un utilisateur
{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>

    <div class="example-wrapper">
        <div>
            <h1 class="text-center">{% if app.request.get('_route') == "edit_user" %}Editer{% else %}Ajouter{% endif %} un utilisateur</h1>
        </div>

        <div>
            {{ form(form) }}
        </div>

        <a class="btn btn-primary btn-lg" href="{{ path('admin_home') }}">Retour</a>

    </div>

{% endblock %}